[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
    'npm install',
    'cd backend && npm install'
]

[phases.build]
cmds = [
    'npx ng build --configuration production --output-path dist/nombre-proyecto',
    'ls -la dist/',
    'ls -la dist/nombre-proyecto/',
    'cd backend && npm run build'
]

[start]
cmd = 'cd backend && npm run start:prod'

[variables]
NODE_ENV = 'production'