{
  "version": 2,
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/nombre-proyecto",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
